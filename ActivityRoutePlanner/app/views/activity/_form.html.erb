<tr class="activity">
  <!--<td width="40px"  align="right"/>
  <!--td width="10px"  align="right">
    <%= # link to remove that activity
	link_to image_tag("arrow_up.png", :border=>0,:height=>"10px"),:controller => :activity,:action=>:move,:direction=>"up",:activity=>activity, :remote => true,:onclick=>'showWall();'%>
    <%= # link to remove that activity
	link_to image_tag("arrow_down.png", :border=>0,:height=>"10px"),:controller => :activity,:action=>:move,:direction=>"down",:activity=>activity, :remote => true,:onclick=>'showWall();'%>
  </td-->
  <td align="right">
<%
label = activity.result!=nil && activity.result.label!=nil ? activity.result.label : "no_name"
if show_delete 
        imageUrl = activity.get_image_url()
%>
	<img src="<%= imageUrl%>"
		alt="<%= activity.point.label%>"
		title="<%= activity.point.label%>"
		height="20px"/>
<%end%>
</td><td width="155px">
  <b><%=label%></b>
<!--%= select_tag "activity_"+activity.id.to_s,
            options_from_collection_for_select(#get available activities and sort by value
            activity.get_possible_values(session).sort {|a,b| a.value <=> b.value} ,"tag_value","value",activity.tag_value),
		  :size=>"1",
		  :style =>"width:156px;",
                  :include_blank => activity.is_empty,
                  :class => 'actionSelectBox',
		  :onfocus=>"this.size=7",
		  :onblur=>"this.size=1",
		  :onmouseenter=>"this.size=7",
		  :onmouseleave=>"this.size=1",	 
	:onchange => 'showWall();'+remote_function(
	:url=> "/add_activity",
	:with => "'id="+activity.id.to_s+"&activity=' +document.getElementById('activity_"+activity.id.to_s+"').value")%-->
  </td><td>
  <%if show_delete %>
	<%= # link to remove that activity
	link_to image_tag("delete.gif", :border=>0,:class => "delete"), "/delete_activity?delete_activity="+activity.id.to_s, :remote => true,:onclick=>'showWall();'%>
<%end%>
</td></tr>