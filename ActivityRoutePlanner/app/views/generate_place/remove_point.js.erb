removeMarks();
<%=Route.find(session[:main_route]).show_markers()%>
var elem = document.getElementById('route_id');
elem.innerHTML = "<%= escape_javascript(render(:partial => 'init/menu'))%>";
hideWall();
