<%route = session[:main_route] %>

<table style="font-size:11px;">
  <tr>
    <td><%= link_to "Reset Route", "/resetMethod" ,:onclick => 'showWall();'%></td>
    <td align="right"><%= link_to "Create Route", "/check_edge", :remote => true, :onclick => 'showWall();'%></td>
  </tr>
<tr>
	<td style="width:120px;">	
Select algorithm:
</td><td>
<%= select_tag "setAlgorithmus",
            options_for_select(Global::ALGORITHMUS_LIST,route.algorithmus),
		:style=>"width:117px;",
	    :onchange => 'showWall();'+remote_function(
		 	 :url=> "/setAlgorithmus",
			 :with => "'algo=' +document.getElementById('setAlgorithmus').value")
%>
</td></tr>
<tr>
	<td style="width:120px;">	
Sort activities:
</td><td>
<%= select_tag "sort",
            options_for_select(["false","true"],route.sort),
		:style=>"width:117px;",
	    :onchange => 'showWall();'+remote_function(
		 	 :url=> "/setAlgorithmus",
			 :with => "'sort=' +document.getElementById('sort').value")
%>
</td></tr>
</table>