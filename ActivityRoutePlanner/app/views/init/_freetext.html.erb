<%#coordinates = point.lat.to_s+":"+point.lon.to_s+":"+index.to_s
  #label = point.label!=nil && !"".eql?(point.label) ? point.label : "no name"%>
<% type="start" %>
<% form_tag "/freetext",
            :id => type+"_form_id",
            :onsubmit => "setBBox('"+type+"_form_id');" do %>

<%=text_field_tag 'freetext', 
                  nil,
                  :placeholder => 'From university bremen to restaurant near Berliner Freiheit', 
                  :id => 'freetext', 
                  :align => "center"
%>
<%= hidden_field_tag :type, type %>
  <%= hidden_field_tag :minlon %>
  <%= hidden_field_tag :minlat %>
  <%= hidden_field_tag :maxlon %>
  <%= hidden_field_tag :maxlat %>
  <%= hidden_field_tag :zoom %>

<% end %>

